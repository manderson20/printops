[project]
name = "printops-syslog-relay"
version = "0.1.0"
description = "UDP syslog collector for printers — parses RFC 3164/5424 messages and posts them to printops-api's internal /syslog/events endpoint"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27",
]

[tool.uv]
package = false
