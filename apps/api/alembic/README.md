# Alembic migrations (placeholder)

No database models or migrations exist yet in this scaffold pass. Once `app/models/`
gains real SQLAlchemy models, initialize Alembic here (`alembic init .`) and wire it
to `app/core/config.py`'s `database_url` setting.
